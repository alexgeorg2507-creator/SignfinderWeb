# TASK: Fix-22 — откатить публикацию юр.документов и гейт, оставить в долге

> Владелец поймал на test: черновой текст (плейсхолдер имени оператора не
> заполнен) оказался и публично доступен (`TASK_fix20.md`), и — хуже —
> заблокировал вход в кабинет (`TASK_fix21.md`). Рано выставлять наружу:
> нет оформленного юрлица в Грузии, некому подписаться под текстом.
> Убрать с test **сейчас**, прод не трогать (там этого никогда не было).

## §1. Срочно — снять блокировку входа (fix21)

Гейт принятия условий должен перестать показываться и пускать в кабинет
как было до fix21. Не откатывать миграцию (`terms_acceptance_log` —
аддитивная, лишняя колонка не мешает) — просто:
- Условие показа гейта — временно всегда `false` (или вырезать вызов
  экрана целиком из потока логина, что проще поддерживать в голове —
  на твой выбор)
- `POST /v1/me/accept-terms` можно оставить как есть, просто никто его
  теперь не вызывает — не ломать, если он уже где-то смёржен

## §2. Снять с публикации три документа (fix20)

- `firebase.json` — убрать rewrites `/terms`, `/privacy`, `/cookies`
  (обратно 404, как было до fix20)
- Убрать ссылки на них из футеров (`app/index.html`, `sign/index.html`,
  `pep-agreement.html`, корневой `index.html`)
- **Файлы `terms.html`/`privacy.html`/`cookies.html` не удалять** —
  черновик текста уже написан, пригодится когда дойдёт очередь. Просто
  больше ничто на сайте на них не ссылается и роутинга нет.

## §3. Зафиксировать как долг, не как «забыто»

`BACKLOG.md` — новая запись: юр.документы и гейт готовы технически,
публикация отложена до оформления легального ИП в Грузии — заполнить
`[ИМЯ/РЕГИСТРАЦИЯ ВЛАДЕЛЬЦА]` в `terms.html`/`privacy.html`, тогда же
решить нужно ли review юриста перед реальным запуском, и только тогда
возвращать fix20/fix21 (снятие §1/§2 в обратную сторону — файлы уже
готовы, работы на 20 минут, не переписывать заново).

## §4. DoD

- [ ] Вход в кабинет на test — без гейта, как до fix21
- [ ] `/terms`, `/privacy`, `/cookies` — снова 404 на test
- [ ] Ссылок на них нигде на сайте не осталось
- [ ] Файлы `.html` физически на месте в репо, не удалены
- [ ] Прод не трогать — там этого не было, трогать нечего
- [ ] `BACKLOG.md` — пункт про парковку с условием возврата
