name: Deploy Web — Prod

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read web version
        run: |
          WEB_VERSION=$(cat version.txt 2>/dev/null || echo "1.0.0")
          echo "WEB_VERSION=$WEB_VERSION" >> $GITHUB_ENV

      - name: Inject version into cabinet HTML
        run: |
          if grep -q '__SF_VERSION__' app/index.html; then
            sed -i "s|window\.__SF_VERSION__\s*=\s*['\"][^'\"]*['\"]|window.__SF_VERSION__ = '${{ env.WEB_VERSION }}';</script></head>|g" app/index.html
          else
            sed -i "s|</head>|<script>window.__SF_VERSION__ = '${{ env.WEB_VERSION }}';</script></head>|" app/index.html
          fi

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS_PROD }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Deploy Firebase Hosting (prod)
        run: |
          npm install -g firebase-tools --silent
          firebase deploy --only hosting --project=signfinder-prod

      - name: Notify Telegram
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          JOB_STATUS: ${{ job.status }}
          GH_RUN: ${{ github.run_number }}
          GH_ACTOR: ${{ github.actor }}
        run: |
          pip install requests -q
          python3 - <<'PYEOF'
          import os, requests
          bot_token = os.environ.get("TELEGRAM_BOT_TOKEN", "")
          chat_id   = os.environ.get("TELEGRAM_CHAT_ID", "")
          if not bot_token or not chat_id:
              exit(0)
          status = os.environ.get("JOB_STATUS", "unknown")
          icon = "\u2705" if status == "success" else "\U0001f6a8"
          text = (
              f"{icon} SignFinder PROD \u2014 Web deployed\n"
              f"Web:  v{os.environ.get('WEB_VERSION','?')}\n"
              f"URL:  https://signfinder.app\n"
              f"Status: {status} | run #{os.environ.get('GH_RUN','?')} by {os.environ.get('GH_ACTOR','?')}"
          )
          requests.post(
              f"https://api.telegram.org/bot{bot_token}/sendMessage",
              json={"chat_id": chat_id, "text": text}, timeout=10,
          )
          PYEOF
