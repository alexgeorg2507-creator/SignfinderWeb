# TASK: E4 — два недостающих факта перед финальным зачётом

## Контекст

Основной отчёт E4 хороший и фактический, но два пункта из
`TASK_deal_cycle_E4.md` §4 не закрыты:

## §1. Визуальная консистентность статус-пилюль

Отчёт даёт текстовые статусы («Черновик», «Контрагент открыл»,
«Подписано»), но не подтверждает что они **визуально** оформлены как
`.traffic-pill`-подобные пилюли с цветовой кодировкой по TASK §2.2:
`draft`/`sent` — нейтральный, `viewed` — жёлтый, `signed` — зелёный,
`expired`/`rejected` — красный.

Пришли: реализовано ли цветовое кодирование, или сейчас просто текст
без цвета? Если реализовано — подтверди как (сделал скриншот/прочитал
computed CSS class через DOM, не важно способ, важен факт).

## §2. Создание сделки именно в рамках этой проверки

DoD прямо просил: «создать новую сделку прямо в рамках этой проверки»
— то есть свежее создание внутри сессии E4-верификации, не переиспользование
черновика `42ec8eaf` из более раннего сеанса. Смысл пункта — убедиться
что список реально живой (нет кэширования/устаревших данных), а не
просто что старые записи корректно отображаются.

Сделай: подпиши ещё один тестовый документ прямо сейчас → убедись что
новая сделка появляется в списке «Мои сделки» (с reload или без —
не критично, зафиксируй как есть) → пришли её `deal_id`.

## §3. Заодно — version.txt

Коротко подтверди: `git diff` по `version.txt` пуст, как в E3 (просто
одна строка, не нужно re-объяснять).

---

## Definition of Done

- [ ] Статус-пилюли — факт про цветовую кодировку
- [ ] Новый `deal_id`, созданный именно сейчас, виден в списке
- [ ] `version.txt` не тронут — подтверждено

Пришли эти три факта — и E4 закрыт полностью.

---

**Автор задачи:** владелец + Claude Opus (2026-07-26)
**Приоритет:** блокер финального зачёта E4, не блокер для чтения дальше документации
