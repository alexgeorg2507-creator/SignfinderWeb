# TASK: E2 — закрыть три открытых вопроса перед финальным зачётом

## Контекст

Получен отчёт: PR #9 в `signfinder-api` (Cyrillic-фикс юр. блока) смёржен,
CI зелёный, деплой на test подтверждён (`api_version: 1.19.2`,
`api_sha: 96fc069`, `core: 1.21.0`). Отдельно заявлено что PR с доками
смёржен и что happy-path верификация (`TASK_deal_cycle_E2_happy_path_verify.md`)
завершена.

**Прежде чем считать E2 закрытым — три пункта требуют явного ответа,
без них зачёт не финальный.**

---

## §1. Репозиторий доков — расхождение в отчёте

В отчёте: «`SignfinderLand` PR #2 (docs completion status) — merged»,
но ссылка указывает на `github.com/alexgeorg2507-creator/**SignPDFMVP**/pull/2`.

Проверено локально: `.git/config` в `C:\work\SignfinderLand\` показывает
`remote origin = https://github.com/alexgeorg2507-creator/SignfinderWeb.git`,
и ветка `docs/e2-completion-status` привязана именно к нему. `SignPDFMVP` —
другой, легаси-репозиторий (старый Streamlit-прототип, к веб-кабинету
отношения не имеет).

**Нужно:** подтвердить явно — это опечатка при копировании ссылки в отчёт
(PR реально в `SignfinderWeb`, просто неверно вставлена ссылка), или PR
действительно был открыт/смёржен не в тот репозиторий. Если второе —
исправить: закрыть PR в `SignPDFMVP` без мержа (если ещё не смёржен) или,
если уже смёржен туда, откатить и открыть заново в `SignfinderWeb`.

Дай точную рабочую ссылку на PR после проверки.

---

## §2. Deal Cycle (E3) в `app/index.html` — вопрос без ответа

Ранее задан прямой вопрос, ответа не было. Повторяю:

При работе над E2 в `C:\work\SignfinderLand\app\index.html` обнаружен код:

```js
// ── Deal Cycle (E3): "передать контрагенту" ─────────────────────────────────
let _currentDeal = null;   // Deal from POST /v1/deals — one per _workFile (see workSign())
```

`TASK_deal_cycle_E2.md` §3.4 явно требовал: «Не трогать `app/index.html`».
E3 не был разрешён к старту.

**Нужно, явно и подробно:**
1. Это просто комментарий-заготовка без реальной логики позади, или в
   `workSign()` (или где-то ещё в файле) уже реально вызывается
   `POST /v1/deals`?
2. Если логика реально есть — что именно она делает, на каком этапе,
   закоммичена ли в `main` уже (через какой PR), или лежит в
   незакоммиченном виде/в фиче-ветке?
3. Почему `app/index.html` тронут вопреки прямому указанию не трогать —
   что было основанием для этого решения на месте?

Это не обязательно проблема по существу (возможно, разумный технический
довесок к E2 — например, создание Deal сразу после подписания, до того
как кнопка передачи подключена к UI). Но раз инструкция была прямая, а
решение отступить от неё принято без согласования — нужна полная
прозрачность, не молчание.

---

## §3. Детали happy-path верификации — только факты, не «done»

Отчёт говорит «happy-path verification report done», но фактических
результатов чек-листа `TASK_deal_cycle_E2_happy_path_verify.md` §1.1-1.5
в сообщении нет. Пришли конкретно:

- §1.1: `deal_id` и `share_token` реально созданной сделки на test
- §1.2: каким способом ввода подписи прошёл (файл/камера/canvas),
  что показал confidence в превью обработки, скриншот или описание
  финального экрана
- §1.3: вывод `GET /v1/deals/{id}` — `status`, что в `audit_log`,
  доступен ли `final-pdf`
- §1.4: что произошло при повторном открытии `/sign/{тот же токен}`
  после `signed`, и что вернул повторный `POST /sign` (подтверди 409
  фактическим кодом ответа, не предположением)
- §1.5: происхождение `core 1.21.0` — теперь ответ частично виден
  косвенно (api version двигался 1.19.1→1.19.2 при неизменном core —
  похоже core не трогался в рамках E2), но подтверди явно одной фразой

---

## §4. После ответа

Только когда все три пункта закрыты фактами (не «сделано», а что
конкретно получено) — E2 считается фактически завершённым, и можно
переходить к обсуждению следующего эпика.

---

**Автор задачи:** владелец + Claude Opus (2026-07-25)
**Приоритет:** блокер финального зачёта E2
